name: deploy gaifaa

on:
  pull_request:
    
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Deploy Server
      uses: appleboy/ssh-action@master
      with: 
        host: ${{ secrets.HOME_ADDRESS}}
        username: ${{ secrets.USER }}
        password: ${{ secrets.PASS }}
        port: 22
        script_stop: true
        script: |
          dir